{
  "name": "qr-saas",
  "version": "1.0.0",
  "private": true,
  "description": "QR Code Generator SaaS Platform",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "npm --prefix apps/web install && npm --prefix apps/web run build && node -e \"const fs=require('fs'); fs.mkdirSync('dist/apps/web',{recursive:true}); fs.cpSync('apps/web/dist','dist/apps/web',{recursive:true});\"",
    "preview": "node server.js"
  },
  "dependencies": {
    "jsqr": "^1.4.0"
  }
}
